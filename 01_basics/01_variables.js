const accountId = 14455
let accountEmail = "av7730748@gmail.com"
var accountPassword = "12345"
accountCity = "Jaipur"

let accountState
// accountId = 2
console.log(accountId)
console.table([accountId, accountEmail, accountPassword, accountCity, accountState]);


// we don't use var in js because it doesn't follow block-scope.
// if you just define a variable and doesn't decalare a value of it, js by default takes the value as undefined.


